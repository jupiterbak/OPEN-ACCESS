#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-opcua-pki/bin/crypto_create_CA.js" "$@"
  ret=$?
else 
  node  "$basedir/../node-opcua-pki/bin/crypto_create_CA.js" "$@"
  ret=$?
fi
exit $ret
